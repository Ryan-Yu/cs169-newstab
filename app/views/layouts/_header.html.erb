<!-- Placeholder header for user sign in -->


<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
   <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path %>">NewsTab</a>
   </div>
   <div>
      <ul class="nav navbar-nav">
        <li><%= link_to "Home", root_path %></li>
        
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false">Article Categories <span class="caret"></span></a>
          <ul class="dropdown-menu" role="menu">
            <% Category.all.each do |category| %>
              <li><a href=<%= category_path(category.id) %>><%= category.name %></a></li>
            <% end %>
            
            <li class="divider"></li>
            <li><a href=<%= articles_path %>>View all articles</a></li>
          </ul>
        </li>
        
        <li><%= link_to "Groups", groups_path %></li>
        <li><%= link_to "All Users", users_path %></li>
        
        <%= render 'layouts/search' %>
     
      </ul>
      
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to 'New Article', new_article_path %>
          <li><%= link_to current_user.first_name, current_user %>
          <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
        <% else %>
          <li><%= link_to "Sign in", new_user_session_path %>
          <li><%= link_to "Sign up", new_user_registration_path %>
        <% end %>
      </ul>
   </div>
  </div>
</nav>



